"""AgentForge Phase 3 red-team platform runtime code.

Named ``redteam/`` (not ``platform/``, which shadows a stdlib module) per
the orchestrator's package-layout decision. ``redteam.harness`` is the
Regression & Validation Harness (P3.10, issue #11); later issues add
sibling packages (``redteam.agents``, etc.) alongside it.
"""
